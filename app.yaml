 runtime: python37

    # [START handlers]
    handlers:
    - url: /.*
      script: auto

    - url: /index.html
      static_files: templates/index.html
      upload: templates/index.html

    - url: /templates
      static_dir: templates

   
    # [END handlers]
